#ifndef _TOOLS_H_
#define _TOOLS_H_

#include <string>

/**
 *@brief Remove the quote signs around the string
 *
 * @param s the string to operate
 * @return std::string the result string without quote signs
 */
std::string removeQuoteSigns(std::string s);

#endif // !_TOOLS_H_